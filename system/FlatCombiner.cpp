#include "FlatCombiner.hpp"

DEFINE_bool(flat_combining, true, "Set flat combining mode: (currently just a bool)");
DEFINE_bool(flat_combining_local_only, false, "disable combining on master (serialize)");
